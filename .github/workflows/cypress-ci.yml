name: Cypress Tests

on: [push]

jobs:
  cypress-electron-tests:
    runs-on: ubuntu-latest
    steps: 
      - name: Get code
        uses: actions/checkout@v4

      - name: install dependencies
        run: npm install

      - name: Run E2E test
        run: npx cypress run
